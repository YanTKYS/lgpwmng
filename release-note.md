# Release Notes

各バージョンで実際に変更された内容を記録します。機能の仕様・使い方は [README.md](README.md) を参照してください。

Vault のデータ形式（`schemaVersion`）を変更したリリースには、その旨を明記しています。
明記のないリリースでは形式は変わっておらず、更新にあたって移行作業は不要です。

## v0.6.0 — Chrome ウェブストア公開準備

Vault の `schemaVersion` は 3 のままで、v0.5.x までに保存したデータ・マスターバックアップ・
共有用ファイルはそのまま利用できます。既存の機能（Vault・暗号化・capture・frame / iframe 対応・
アカウント共有・popup・setup・options・URL 照合・管理者確認）の仕様は変更していません。
Chrome ウェブストアの審査・公開・更新運用に必要な説明とパッケージングを整備し、
公開要件への適合として「初回のデータ利用開示と明示的な同意」を追加しました。

- **初回のデータ利用開示と明示的な同意を追加しました（公開要件への適合）。**
  拡張アイコンを初めて押すと、popup に「lgpwmng のデータ利用について」が表示されます。
  扱う情報（現在のページの URL・ログインフォームの入力欄の情報・設定時に入力済みの値。
  ユーザーID・パスワード等を含む）と用途、端末内でのみ処理・保存し外部送信しないことを示し、
  「同意して利用を開始」を押すまで先へ進みません。
  - **同意するまで、現在のタブの URL を取得しません**（`chrome.tabs.query()` を呼びません）。
    ページへのコード注入（走査・取り込み・入力・強調表示）も行いません。
  - 画面側の制御だけに頼らず、Service Worker 側でも `SERVICE_MATCH` / `PAGE_SCAN` /
    `PAGE_CAPTURE` / `PAGE_HIGHLIGHT` / `FILL_RUN` の各要求で同意済みかを確認します。
  - 同意の記録は `chrome.storage.local` の `privacyConsentVersion`（現在 1）と記録日時だけで、
    認証情報は含みません。扱うデータの内容を変えた場合はバージョンを上げて再同意を求めます。
  - 同じ内容を設定ページの「この拡張について」へ常設し、現在の同意状態も表示します。
  - 既存の利用者は、更新後に一度だけこの画面が表示されます。登録済みのデータには影響しません。

- **プライバシーポリシーを追加しました。** リポジトリ直下の `PRIVACY.md` を出典とし、
  GitHub Pages 用の `docs/privacy.html` を `node tools/build-docs.mjs` で生成します。
  内容がずれていないことは `npm test` で検査します。
- **審査用デモページを追加しました。** `docs/reviewer-demo.html`（frame を使わない基本ケース）と
  `docs/reviewer-demo-frame.html`（iframe 内フォームの追加ケース）。架空の業務システムを模した
  ダミーのログイン画面で、実在する組織名・ドメイン・認証情報は使用していません。
  `docs/` は GitHub Pages で公開できる構成にしました（`docs/index.html`）。
- **Chrome ウェブストアの申告資料を `docs/webstore/` へ整理しました。**
  single purpose、権限ごとの利用理由、Privacy practices 申告案、リモートコードの申告、
  ストア掲載文案、スクリーンショット撮影計画、アイコン点検結果、審査コメント欄へ貼る文、
  申請チェックリスト。審査手順は `docs/webstore-review.md` にまとめています。
- **公開用 ZIP を生成できるようにしました。** `npm run package` で
  `dist/lgpwmng-webstore-v0.6.0.zip` を作成します。ZIP 直下に `manifest.json` があり、
  中身は `manifest.json` / `icons/` / `src/` だけです（`docs/` `test/` `tools/` `.github/` は
  含みません）。生成は Node 標準ライブラリのみで行い、外部依存を追加していません。
- **GitHub Actions（`webstore-package`）を追加しました。** `main` への push、タグ、
  pull request で `npm test` → バージョン確認 → 公開用 ZIP 生成 → artifact 化を行います。
- **公開準備に関する自動テスト（`test/package.test.js` / `test/consent.test.js`）を追加しました。**
  manifest の妥当性・Manifest V3・権限が `activeTab` / `scripting` / `storage` のみで
  `host_permissions` がないこと・`<all_urls>` の不在・CSP がリモートコードを許可しないこと・
  外部通信 API とリモートコード参照の不在・`console` 出力の不在・公開用 ZIP の中身・
  Privacy Policy と審査用デモの存在・バージョン整合を検査します。あわせて、popup の同意確認が
  `chrome.tabs.query` より前にあること、ページへ触れる 5 つの要求が同意を必須にしていることも
  検査します。
- **manifest の `description` を見直しました。** 「自治体専用」と受け取られないよう、
  実際の用途（業務システムのログイン入力補助）に即した説明へ変更しました（132 文字以内）。
- **README に Chrome ウェブストア・プライバシー・審査用デモへの導線を追加しました。**
  詳細は `docs/webstore/` へ分離し、README は肥大化させていません。
- 権限・リモートコード・外部通信・認証情報のログ出力を総点検しました。追加の権限はなく、
  外部通信を行うコード・リモートコードの参照・`console` 出力はいずれも存在しませんでした
  （修正が必要な箇所はありませんでした）。
- 申告資料をレビュー指摘に合わせて修正しました。**Small promo tile（440x280）は任意ではなく
  必須**であるため、掲載画像の一覧・チェックリスト・素材の置き場をすべて必須へ揃え、
  作り方の指針を追加しました。また、Developer Dashboard の Data usage 申告で
  **Web history もチェックする**ことにしました。lgpwmng は閲覧履歴を収集しませんが、
  対象ログイン画面の URL（オリジン + パス）を読み取り・保存・照合するため、
  Chrome ウェブストアの「web browsing activity」の定義に該当します。申告・プライバシー
  ポリシー・実際の挙動が食い違わないよう、`PRIVACY.md` に URL の扱いを明示する節を追加し、
  資料間で申告内容がずれないことを `npm test` で検査するようにしました。
  カテゴリは、公式のカテゴリ定義が password safe を例示している
  `Privacy & Security` を第一候補に変更しました（`Workflow & Planning` も可）。
- 自動テストを 87 件へ増やしました（`test/consent.test.js` と、同意まわりの静的検査を追加）。

## v0.5.4 — ドキュメントとバージョン表示の整理

- README・リリースノートから、過去バージョンの経緯・重複した説明・機能範囲外の列挙を削除し、
  現行仕様と使い方が中心になるよう整理しました。「既知の制限」は、知らないと正常に利用できない
  制約（Shadow DOM・画面遷移型ログイン・動的生成の入力欄）だけに絞りました。
- 設定ページ「この拡張について」のバージョン表示を `manifest.json` から取得するようにしました。
  リリースのたびに HTML 側を書き換える必要がなくなり、表示が実際のバージョンとずれなくなります。

## v0.5.3 — 設定・共有画面の不具合修正

- **入力欄の自動判定で、隣り合う手掛かりがつながって別の項目名と誤判定される問題を修正しました。**
  `id` / `name` / ラベル等を連結してから空白・`_`・`-` をまとめて除去していたため、手掛かりの区切りまで
  消えていました（例: `name="login"` とラベル「ID2」が `loginid2` という一語として扱われる）。
  手掛かりごとに整えてから並べます。
- **新規登録時に「再スキャン」すると、行が指す入力欄と画面に表示された取り込み値がずれる問題を
  修正しました。** 確定済みの項目についても、取り直した値を画面へ反映します
  （利用者が自分で編集した項目は従来どおり上書きしません）。
- **共有用ファイルの取り込みで、パスフレーズ欄の Enter が効かない問題を修正しました。**
  他の画面（アンロック・バックアップ復元）と同じく `form` として扱います。
- **共有用ファイルの取り込みで、パスフレーズを間違えると入力内容が消える問題を修正しました。**
  復号に失敗した場合は入力内容を残し、取り込み対象が確定した時点でクリアします。
- 共有用ファイルの作成画面を、出力せずに閉じた場合（「閉じる」・Esc）も入力済みのパスフレーズを
  画面に残さないようにしました。
- 暗号化ファイル（バックアップ／共有用）の外枠の検証を `lib/crypto.js` の `isEncryptedEnvelope()` へ
  一本化しました（`vault-store.js` と `share.js` に重複していました）。

## v0.5.2 — 設定画面の不具合修正

- **既存サービスの設定で「対象入力欄」を選び直すと、登録済みのサービス共通値（自治体コード等）が
  消える問題を修正しました。** 取り込み値の反映を、まだ一度も保存していない新規サービスの下書きに
  限定しました。
- **新規登録時のサービス名の初期値に、ログイン画面のページタイトルを使うようにしました。**
  初期値の決定を 1 か所にまとめ、ページタイトル → ホスト名の順で採用します（従来は常にホスト名でした）。
- **「対象URL」のオリジン欄に解釈できない文字列を入力すると、入力内容が消えて空欄になる問題を
  修正しました。** 解釈できなかった入力はそのまま残します（形式の誤りは保存時に表示します）。
- **サービス一覧の URL 条件で、パス `/` の前方一致が `https://example.jp//*` と二重スラッシュで
  表示される問題を修正しました。**
- 再スキャン後、行が指す入力欄と取り込み済みの値の対応を取り直すようにしました。
- 共有ファイルの取り込み結果で、0 件の内訳（「0アカウントを更新」）を並べないようにしました。
- 使われていない `countSelectedAccounts()` を削除し、`summarizeService()` を画面が実際に使う項目に
  絞りました。`renderValueFields()` の引数をオプション形式に統一しました。
- 自動テストに `test/match.test.js` / `test/model.test.js` を追加しました。

## v0.5.1 — 共有インポート後の再読込漏れとプレビュー表示の修正

- **共有インポート後、設定ページのサービス一覧・編集中のサービスを読み直すようにしました。**
  従来は取り込みが成功しても options.js 側を再読込していなかったため、編集画面が古い accounts を
  保持し続け、その後のアカウント自動保存で**取り込んだアカウントを再び消す**可能性がありました。
  `createSharePanel({ onImported })` を追加して再取得します（再読込の失敗は取り込み結果の表示に
  影響しません）。
- **共有用ファイル作成画面で、サービス単位の「全選択/解除」でも管理者アカウントの警告が
  更新されるようにしました。** 選択件数と管理者警告の更新を `renderExportState()` に一本化しました。
- **共有インポートのプレビューに、サービス設定の更新内容も表示するようにしました。**
  既存サービスへの取り込みでは `matchRules` / `fields` / `sharedValues` も更新されるため、
  プレビューに「サービス設定 → 更新」の行を追加しました（`buildImportPlan()` に `settingsUpdated` を追加）。

## v0.5.0 — 選択アカウントの共有用エクスポート／インポート

一部のアカウントだけを別担当者へ受け渡せる「アカウント共有」を追加しました。
マスターバックアップ（Vault 全体の保全・復旧）とは独立した機能です（詳細は README 9.）。

- 設定ページの「マスターパスワード / バックアップ」タブに「アカウント共有」セクションを追加し、
  「共有用ファイルを作成」「共有用ファイルを取り込む」から操作できるようにしました。
  取り込みは保存前にプレビュー（資格情報の値は非表示）を表示します。
- 共有ファイルは独立した形式（`lgpwmng-share`、`version: 1`）です。バックアップ形式
  （`lgpwmng-backup`）とは読み込み時に判別し、取り違えを拒否します。暗号化は既存の
  PBKDF2 + AES-GCM を再利用しています。
- 共有用パスフレーズは `chrome.storage` / `localStorage` / `sessionStorage` / Vault のいずれにも
  保存せず、処理中だけ入力欄で保持し、完了後にクリアします。
- インポートは Vault 全体を置換せず、`service.id` / `account.id` / `field.id` を基準に追加・更新のみを
  行います。個人サービス・個人アカウント・共有対象外のアカウント・受取側だけが持つ項目は保持されます。
- 新しいロジックは `src/lib/share.js` に分離しました（選択データの抽出・共有ファイルの検証・
  インポート計画の作成・マージ）。復号したデータは既存の `normalizeVault()` / `normalizeService()` を
  通して正規化します。
- 自動テスト（`test/share.test.js` / `test/vault-store.test.js`）を追加しました。

## v0.4.3 — capture 値の重複反映防止

- capture 結果と scan 候補の双方で入力欄を一意に特定できる場合だけ、取得した値を初期反映するように
  しました。同名入力欄の片方だけに値がある場合、その値が別の空欄へ複製される問題を修正しています。
- 曖昧なため値を反映しなかった場合は、取り込み成功メッセージを表示しないようにしました。

## v0.4.2 — リリース履歴の分離

- README から各バージョンのリリース履歴を `release-note.md` へ分離しました。機能の変更はありません。

## v0.4.1 — 入力済み値取り込みの安全性・編集保護

- frame、要素種別、フォーム位置、CSS パス、id、name を組み合わせ、capture 結果が一意な場合だけ
  初期値を反映するようにしました。一意に特定できない場合は、秘密項目を含め手入力を優先します。
- 文字入力、貼り付け、削除、空欄化を明示的な利用者編集として記録し、候補変更や再描画の後も
  capture 値で上書きしないようにしました。
- アカウント項目の capture 値は新規サービスの「標準ユーザー」だけへ反映し、後から追加した
  アカウントには複製しないようにしました。

## v0.4.0 — ログイン画面の入力済み値取り込み

新しいサービスを登録するときの二重入力（ログイン画面への入力 → 設定ページへの再入力）を解消しました
（詳細は README 5.5）。

- 走査（scan）とは別の処理（capture）として実装し、常駐監視は追加していません。値を取得するのは、
  「このログイン画面を設定」を実行した直後の 1 回だけです。
- 値の対応付けには、走査候補と同じ `id` → `name` → CSS セレクタの照合とフレーム情報を使います。
  空欄の入力欄は取り込みません。
- 取得した値はメモリ上の下書きとして扱い、`chrome.storage` 等へ平文で保存することはありません。
- 既存サービスの設定を編集する場合は取得しません（登録済みの値を無確認で置き換えないため）。

## v0.3.3 — 不具合修正と整理

**不具合修正**

- **チェックボックス / ラジオボタンの項目で、入力時に `input` / `change` を二重に発火していたのを
  修正しました。** クリック操作自体がこれらのイベントを発火するため、`onchange` で入力チェックを行う
  業務システムで検証が二度走っていました。
- **ラジオボタンの項目で、登録値と一致しないのに「入力しました」と表示していたのを修正しました。**
  この場合は入力できなかったものとして表示し、状態が変わらないクリックはページへ送りません。
- **項目名の自動判定が効かない画面があったのを修正しました。** `<label><input></label>` のように
  文字を持たないラベルで手掛かり探しが打ち切られていたため、中身のある文字列だけを採用します。
  入力欄の再特定の精度も上がります。
- **ログイン画面設定ページで、設定を読み込めなかったときに「再スキャン」「保存」が無反応になる
  不具合を修正しました。** 読み込めていない旨を表示します。
- **「アカウントを追加」の既定名が実際の件数とずれていたのを修正しました。** 使われていない
  最も小さい番号を使います。
- **アカウントの自動保存で、保存していないのに「保存済み」と表示することがあったのを修正しました。**

**整理（動作は変わりません）**

- 「確認」（強調表示）が失敗したときの案内を理由ごとの一覧にまとめ、フレーム構成を確認できなかった
  場合に「入力欄が見つかりません」と誤って案内していたのを改めました。
- ページ内処理の呼び出しを `runPageAgent()` にまとめ、全フレーム実行・単一フレーム実行を
  `runInAllFrames()` / `runInFrame()` の薄い呼び分けにしました。
- 走査できなかったフレームの判定を `hasUnreachableFrames()` として切り出しました。
- 値の反映処理（`applyValue()`）を、入力欄の種類ごとに読み取れる形へ整理しました。

## v0.3.2 — 不具合修正と整理

**不具合修正**

- **対応付けが弱い項目が、他の項目の入力欄を先取りしてしまう不具合を修正しました。**
  確実に特定できた項目から先に割り当てます（従来は登録順に割り当てていたため、値が入れ替わって
  入力される場合がありました）。
- **URL 条件のパスに先頭の `/` を付け忘れると、どのページにも一致しなくなる不具合を修正しました。**
  `login` は `/login` として扱い、誤って付けたクエリ（`?` 以降）も取り除きます。
- **「確認」（強調表示）を続けて押すと、オレンジ色の枠が消えなくなる不具合を修正しました。**
- **既存サービスの設定を開いたとき、1 つの入力欄が複数の項目に割り当たることがあったのを
  修正しました。** そのままでは保存時にエラーとなり、手作業で直すまで保存できませんでした。
- **サービス一覧のアカウント件数が、アカウントの自動保存後も古いままだったのを修正しました。**

**整理（動作は変わりません）**

- 入力項目を組み立てる処理が「保存時」と「描画時」に二重に書かれていたのを `collectFields()` に
  まとめました。
- フレームの再特定を `probeFrames()`（構成の確認）と `matchFrames()`（該当フレーム探し）に分け、
  入力 1 回につきフレーム構成の確認を 1 度だけ行うようにしました。入力処理も `fillFrameGroup()` に
  統合しました。

## v0.3.1 — 不具合修正と整理

**不具合修正**

- **入力欄を特定しきれずに見送った秘密項目の入力欄へ、別項目の値が入ることがある不具合を
  修正しました。** 項目に割り当てた入力欄は、入力の可否によらず他の項目へ渡しません。
- **入力時に `blur` を二重に発火していたのを修正しました。** `blur()` の呼び出し自体が blur /
  focusout を発火するため、合成イベントは送りません。
- **入力結果の一覧が登録した項目の順に並ばない不具合を修正しました。** 登録順に並べ直し、
  項目と結果が 1 対 1 で対応するようにしています。
- **入力項目の表示名を打つたびにアカウント欄が作り直される問題を修正しました。** 値の入力欄は、
  項目の内容が確定した時点で作り直します。
- **入力欄の候補が多い画面で設定画面が固まる問題を改善しました。** 「対象入力欄」の選択肢を
  候補 1 件につき 1 回だけ組み立て、各行へ複製します。
- **ログイン画面設定ページの読み込み失敗時に、原因によらず「ロックされています」と表示していたのを
  修正しました。** アンロックしても解決しない原因はそのまま表示します。
- **「アカウントを追加」の既定名が既存のアカウントと重複することがあったのを修正しました。**

**整理（動作は変わりません）**

- `match.js` と `frame.js` で重複していた URL の解釈とパスの正規化を `lib/url.js` に集約しました。
- アカウント自動保存の値の組み立て（`buildSafeAccountsPayload`）を、「直前保存時の値を土台にして、
  区分が変わっていない項目だけ現在の値で上書きする」という一つの規則に書き直しました。
- `frame.js` に紛れ込んでいた制御文字（NUL）を通常の区切り文字に置き換えました。ソースがバイナリ
  扱いになり、差分を確認できなくなっていました。

## v0.3.0 — フレーム内ログインフォーム対応

`frameset` / `frame` を使った古い業務システムや `iframe` でログインフォームを埋め込んだ画面に
対応しました（詳細は README 5.4）。既存項目にフレーム情報が無い場合は自動的にトップフレーム扱いと
して読み込まれるため、`schemaVersion` は変更していません。

- 走査（スキャン）が全フレームを対象になりました。`<frame>` と `<iframe>` は区別しません。
- 項目ごとにフレーム情報（origin・パス・フレーム名）を保存し、入力の都度、現在のフレーム構成から
  対象フレームを再特定するようにしました。Chrome の frameId は保存データに使っていません。
- 同じ推定名の入力欄が複数フレームにまたがる場合は自動で確定せず、候補一覧に `[フレーム: ○○]` を
  表示して利用者が選べるようにしました。
- **フレームを一つに絞り込めない場合、秘密項目は入力しません（fail closed）。** 通常項目のみ、
  参考として弱い一致で入力を試みます。
- 子フレームへ入力する場合は、そのフレーム自身の URL（origin + パス）も入力直前に再確認するように
  しました（トップページの二重確認はそのまま維持しています）。
- 「強調表示」「再スキャン」も全フレーム対応にしました。再スキャンは frameId に頼らず、フレーム自身の
  URL・名前で既存の対応付けをできるだけ保持します。
- 走査したフレーム数が想定より少ない場合は、候補 0 件と区別して「一部のフレームを確認できませんでした」
  と表示するようにしました。

## v0.2.2 — 自動保存とサービス設定の状態分離

- **未保存の入力項目変更が、アカウントの自動保存に巻き込まれないようにしました。** 入力項目の区分変更・
  削除は明示保存が前提のため、保存されるまでの間に自動保存が走っても、影響を受けた値は直前に
  保存済みの内容のまま送ります。
- **明示的な「保存」は、クリックした時点の内容で確定してから送るようにしました。** 保存クリックが
  後から編集した内容まで巻き込むことはありません。
- 判定・復元ロジック（`buildSafeAccountsPayload`）と保存直後のスナップショット作成
  （`snapshotService`）を `src/ui/account-editor.js` に共通化しました。

## v0.2.1 — アカウント変更の自動保存

- **保存済みのサービスでは、アカウントの追加・削除・名前変更・通常/管理者区分の変更・各項目値の
  変更を自動保存するようにしました。** テキスト入力は確定（`change` / フォーカスが外れたタイミング）で、
  追加・削除・区分変更はその場で保存します。
- **対象URL・入力項目・サービス共通値・サービス名は、従来どおり明示的な「保存」のままです。**
  新規サービスは最初の 1 回だけ明示保存が必要です。
- アカウント領域に保存状態表示（「保存中…」「保存済み」「保存できませんでした [再試行]」）を
  追加しました。失敗した入力内容が画面から消えることはありません。
- 保存処理を直列化し、古い保存処理が後から完了して新しい値を上書きしないようにしました。
- 自動保存のトリガーと直列保存キューを `src/ui/account-editor.js` に共通化しました。

## v0.2.0 — アカウント管理導線の再設計

- **ログイン画面設定ページ（setup.html）だけでアカウントまで設定できるようにしました。**
  従来は 2 件目以降のアカウントを追加するのに「サービス一覧」（options.html）へ移動する必要が
  ありました。
- **サービス一覧（options.html）で、アカウントを最初に表示するようにしました。** 対象URL・入力項目・
  サービス共通値は「サービス設定」として折り畳み、初期状態では閉じています。
- アカウント一覧の描画・追加・削除・値編集・秘密項目の表示切り替えを `src/ui/account-editor.js` に
  まとめ、設定ページとサービス一覧の双方から利用するようにしました。

## v0.1.4 — 不具合修正と整理

- **ログイン画面設定ページで「保存」を 2 回押すと、登録した値がすべて消えていた問題を修正しました。**
  保存後は項目の対応付けを保持します。
- **同じ画面で「保存」を続けて押すと、同じサービスが二重に登録されることがあった問題を修正しました。**
  保存中はボタンを押せないようにしています。
- **項目の区分（共通 / アカウント）を変更すると、アカウントごとに登録していた値が失われていた問題を
  修正しました。** 変更前の区分の値を残すため、区分を戻せば元のまま使えます。
- **「再スキャン」すると、入力中の項目や手動で追加した項目が消えていた問題を修正しました。**
  行は作り直さず「対象入力欄」の選択肢だけを更新します。
- **入力欄の多いログイン画面で、走査・入力に時間がかかり画面が固まったようになる問題をさらに
  改善しました。** 入力欄ごとにページ全体を数え直していた処理をやめています（入力欄 300 件の画面で
  約 1/4 の時間）。入力欄の識別情報は従来と同一です。
- **ログイン画面設定ページで対象URLの条件を書き換えても保存されなかった問題を修正しました。**
- 走査結果をタブごとに保持し続けていたのをやめ、直前の 1 件だけを保持するようにしました。
  別のタブの走査結果が設定ページへ渡ることもなくなります。

## v0.1.3 — 不具合修正と整理

- **入力欄の多いログイン画面で、入力に数秒かかり画面が固まったようになる問題を修正しました。**
  入力欄の識別情報を項目ごとに作り直していたのをやめ、1 回だけ作って使い回します
  （入力欄 300 件の画面で約 1.9 秒 → 約 0.3 秒）。入力先の判定結果は従来と同じです。
- **「再スキャン」すると入力済みの値が消えていた問題を修正しました。** 表示名が同じ項目の値を
  引き継ぎます。
- **項目の区分（共通 / アカウント）を変更すると、登録済みの値が失われていた問題を修正しました。**
  変更後の区分側へ値を引き継ぎます。
- **サービス一覧からの保存時に、サービス名・オリジンが未入力でも保存できてしまう問題を
  修正しました。** オリジン欄の正規化は入力途中ではなく確定時に行い、結果を画面へ反映します。
- **設定画面の「確認」ボタンが、入力欄を特定できなかった場合でも成功と表示していた問題を
  修正しました。**
- popup の表示を改善しました。対象ページをプロトコル込み（`https://…`）で表示し、サービス /
  アカウントの切り替え時に前回の入力結果を消します。入力先が確実でない項目がある場合は、その件数を
  入力結果に表示します。
- バックアップ復元時、ファイルの形式が違う場合に「パスフレーズが違います」と誤って案内していた点を
  改めました。
- バックアップファイル名の日時を端末のローカル時刻にしました。
- サービス一覧の操作中にロックされた場合など、処理に失敗した際の内容を画面へ表示するようにしました。

## v0.1.2 — 旧 URL 条件の実 URL ベース自動移行

**`schemaVersion` を 3 へ変更しました**（プロトコル未確定の旧条件を `legacy` として保持できます）。

- v0.1.0 形式（プロトコル情報を持たない `hostname`）の URL 条件を `https` とみなす暗黙変換を
  廃止しました。lgpwmng がプロトコルを推測することはありません。
- プロトコル未確定の条件はそのまま保持し、対象のログイン画面を実際に開いた時点で、そのページの
  オリジンを採用して新形式へ自動移行し、Vault へ保存します。サブドメイン一致の条件も意味を保って
  移行します。
- 現在のページが旧条件に一致しない場合は、条件を書き換えず、認証情報も入力しません。

## v0.1.1 — 認証情報入力時の安全性補強

**URL 判定を `hostname + pathname` から `origin + pathname` へ変更しました**（`schemaVersion` 2）。
プロトコルとポートを含めて判定するため、`http` と `https` を区別します。

- 入力の直前に URL を再確認するようにしました。background で現在のタブ URL を取得してサービスの
  条件と照合し、注入したコード自身もページの `location` を再確認します（不一致・URL 不明なら
  入力しません）。
- 秘密項目（種別「秘密」）は、入力欄の特定が確実な場合のみ入力するようにしました。一致が弱い場合は
  入力せず、設定の更新を促します（通常項目は従来どおり警告付きで入力します）。

## v0.1.0

- 初版。

# Release Notes

## v0.5.3 — 設定・共有画面の不具合修正と整理

日常業務での利用中に支障となる不具合を修正し、あわせて重複していた検証処理を整理しました。
Vault のデータ形式および `schemaVersion` は変更していません。

- **入力欄の自動判定で、隣り合う手掛かりがつながって別の項目名と誤判定されることがある問題を
  修正しました。** 判定は `id` / `name` / ラベル等の手掛かりを並べた文字列で行いますが、
  連結してから空白・`_`・`-` をまとめて取り除いていたため、手掛かりの区切りまで消えていました。
  その結果、例えば `name="login"` とラベル「ID2」の組み合わせが `loginid2` という一語として
  扱われるなど、単独では一致しないはずのキーワードに一致することがありました。
  手掛かりごとに整えてから並べるようにし、区切りが残るようにしています
  （「ユーザー ID」→「ユーザーid」のような 1 つの手掛かり内の整形は従来どおりです）。
- **新規登録時に「再スキャン」すると、行が指す入力欄と画面に表示された取り込み値がずれる問題を
  修正しました。** 再スキャンで候補の並びが変わったとき、行が保持する取り込み値は取り直して
  いましたが、既に確定済みの項目については画面（アカウント値・共通値）へ反映し直していなかった
  ため、行が別の入力欄を指すようになった後も再スキャン前の値が残り、そのまま保存される
  可能性がありました。取り直した値を確定済みの項目にも反映します
  （利用者が自分で編集した項目は、これまでどおり上書きしません）。
- **共有用ファイルの取り込みで、パスフレーズ欄で Enter を押しても何も起きない問題を
  修正しました。** ファイル選択とパスフレーズの入力欄が `form` になっていなかったため、
  他の画面（アンロック・バックアップの復元）と操作感が違っていました。`form` として扱い、
  Enter でも「内容を確認」できるようにしています。
- **共有用ファイルの取り込みで、パスフレーズを間違えると入力内容が消える問題を修正しました。**
  1 文字の打ち間違いでも、長いパスフレーズとファイル選択をすべて入れ直す必要がありました。
  復号に失敗した場合は入力内容を残し、取り込み対象が確定した時点でクリアします。
- 共有用ファイルの作成画面を、出力せずに閉じた場合（「閉じる」・Esc）も入力済みのパスフレーズを
  画面に残さないようにしました（取り込み画面と同じ扱いに揃えています）。
- 整理: 暗号化ファイル（バックアップ／共有用）の外枠の検証が `vault-store.js` と `share.js` に
  重複していたため、`lib/crypto.js` の `isEncryptedEnvelope()` へ一本化しました。

## v0.5.2 — 設定画面の不具合修正と整理

日常業務での利用中に支障となる不具合を修正し、あわせて不要になっていたコードを整理しました。
Vault のデータ形式および `schemaVersion` は変更していません。

- **既存サービスの設定で「対象入力欄」を選び直すと、登録済みのサービス共通値（自治体コード等）が
  消えてしまう問題を修正しました。** ログイン画面の入力済み値の取り込み（capture）は新規登録時
  だけの機能ですが、「対象入力欄」を変更したときの値の差し替え処理が、取り込みを行っていない
  既存サービスの編集でも動いていました。その結果、区分が「共通」の項目で対象入力欄を選び直すと、
  取り込み値（空）で上書きされ、登録済みの共通値が画面上から消えた状態になり、そのまま保存すると
  値が失われていました。取り込み値の反映を、まだ一度も保存していない新規サービスの下書きに
  限定しました（保存後・既存サービスの編集では、登録済みの値が capture の結果で変わることはありません）。
- **新規登録時のサービス名の初期値に、ログイン画面のページタイトルを使うようにしました。**
  ページタイトルを初期値にする処理はありましたが、その前にホスト名を初期値として設定していたため
  条件が成立せず、実際には常にホスト名（例: `sys.example.lg.jp`）が入っていました。初期値の決定を
  1 か所にまとめ、ページタイトル → ホスト名の順で採用します（いずれも利用者が上書きできます）。
- **設定ページの「対象URL」で、オリジン欄に解釈できない文字列を入力すると、入力内容が消えて
  空欄になる問題を修正しました。** 何を入力したのか分からなくなり修正しようがなかったため、
  解釈できなかった入力はそのまま残すようにしました（形式の誤りは、これまでどおり保存時に
  「オリジンを「https://ホスト名」の形式で入力してください。」と表示します）。
- **サービス一覧の URL 条件の表記で、パスが `/` の前方一致が `https://example.jp//*` と
  二重スラッシュで表示される問題を修正しました**（`https://example.jp/*` と表示します）。
- **再スキャン後、行が指す入力欄と取り込み済みの値の対応を取り直すようにしました。**
  候補の並びが変わっても、後から確定した項目に古い候補の値が入ることがなくなります。
- 共有ファイルの取り込み結果で、0 件の内訳（「0アカウントを更新」）を並べないようにしました。
- 整理: 使われていない `countSelectedAccounts()` を削除し、一覧向けの要約（`summarizeService()`）を
  画面が実際に使う項目だけに絞りました。項目値の描画（`renderValueFields()`）の引数を
  オプション形式に統一し、`undefined` を渡す呼び出しを無くしました。
- 自動テストに `test/match.test.js` / `test/model.test.js` を追加し、URL 判定・条件の表記・
  入力値の組み立て・要約・区分変更時の値の引き継ぎを検証しています。

## v0.5.1 — 共有インポート後の再読込漏れとプレビュー表示の修正

v0.5.0 のレビューで指摘された、アカウント共有機能の不具合を修正しました。
Vault のデータ形式および `schemaVersion` は変更していません。

- **共有インポート後、設定ページのサービス一覧・編集中のサービスを読み直すようにしました。**
  これまでは共有ファイルの取り込みが成功しても options.js 側の状態を再読込しておらず、
  取り込み前から特定のサービスを開いたまま共有インポートを実行すると、開いていた編集画面が
  古い accounts を保持し続けていました。その状態でアカウントを編集すると、自動保存が
  古い accounts の内容で保存してしまい、**直前の共有インポートで追加・更新したアカウントを
  再び消してしまう**可能性がありました。`createSharePanel({ onImported })` としてコールバックを
  追加し、取り込み成功後に一覧と編集中サービスを Vault から再取得するようにしました
  （再読込自体が失敗しても、既に成功している取り込み結果の表示には影響しません）。
- **共有用ファイル作成画面で、サービス単位の「全選択/解除」でも管理者アカウントの警告が
  正しく更新されるようにしました。** 個別のチェックボックスでは警告表示を再評価していましたが、
  「全選択/解除」は一覧を再描画するだけで警告の再評価を呼んでいなかったため、管理者アカウントを
  一括選択しても「管理者アカウントを含みます」の警告が表示されないことがありました。
  選択件数と管理者警告の更新を `renderExportState()` に一本化し、個別選択・全選択・初期描画の
  すべてから呼ぶようにしました。
- **共有インポートのプレビューに、サービス設定の更新内容も表示するようにしました。**
  既存サービスへの取り込みでは、実際には account の追加・更新に加えて `matchRules` / `fields` /
  `sharedValues` も共有側の内容でマージ更新されますが、従来のプレビューは account の増減しか
  示していませんでした。既存サービスの場合はプレビューに「サービス設定 → 更新」の行を追加し、
  実際の処理内容と一致させました（`buildImportPlan()` が返す各サービスへ `settingsUpdated` を追加）。
- 自動テストに、`buildImportPlan()` が既存サービスでは `settingsUpdated: true`、新規サービスでは
  `settingsUpdated: false` を返すことを検証するケースを追加しました。

## v0.5.0 — 選択アカウントの共有用エクスポート／インポート

システム管理担当者・複数担当者で共用している業務アカウント・管理者用の共通アカウントなど、
一部のアカウントだけを別担当者へ安全に共有できるようにしました。既存のマスターバックアップ
（Vault 全体の保全・復旧）とは明確に分離した、独立の機能です。

- **新しい「アカウント共有」機能を追加しました。** 設定ページの「マスターパスワード / バックアップ」
  タブに、マスターバックアップとは別の「アカウント共有」セクションを設け、「共有用ファイルを作成」
  「共有用ファイルを取り込む」から操作します。
  - **共有用ファイルを作成**: サービスごとに、共有するアカウントだけをチェックボックスで選択します
   （サービス単位の全選択/解除にも対応）。チェックしなかったアカウント（個人アカウント等）は
    ファイルに含まれません。選択に管理者（role = admin）アカウントが含まれる場合は、出力前に
    「管理者アカウントを含みます。共有先と取扱いを確認してください。」と表示します。
    共有用パスフレーズ（8文字以上、マスターパスワードとは独立）を 2 回入力して一致した場合のみ、
    `lgpwmng-share-YYYYMMDDhhmmss.json` を出力します。
  - **共有用ファイルの取り込み**: ファイルとパスフレーズを指定すると、保存前に「◯◯システム /
    システム管理 → 更新」のような追加・更新内容のプレビュー（資格情報の値は非表示）を表示し、
    内容を確認してから取り込みを実行します。完了後は「2件のアカウントを追加し、1件を更新しました。」
    のように結果を表示します。
- **共有ファイルは Vault 全体とは異なる独立した形式（`lgpwmng-share`、`version: 1`）です。**
  既存のバックアップ形式（`lgpwmng-backup`）とは読み込み時に判別され、バックアップファイルを
  共有用インポートで読み込んだり、共有ファイルをバックアップ復元で読み込んだりすることはできません
  （それぞれ適切なメッセージを表示して拒否します）。暗号化は既存の PBKDF2 + AES-GCM（Web Crypto API）
  をそのまま再利用しており、平文 JSON として出力されることはありません。
- **共有用パスフレーズは保存しません。** `chrome.storage.local` / `chrome.storage.session` /
  `localStorage` / `sessionStorage` / Vault のいずれにも保存せず、エクスポート／インポートの
  処理中だけメモリ上（入力欄）で保持し、処理完了後は入力欄もクリアします。
- **共有ファイルには、選択したアカウントが利用できるよう、対象サービスの定義
  （サービスID・サービス名・URL条件・入力項目定義・locator・frame情報・field ID・
  `sharedValues`（自治体コード等のサービス共通値）・選択したアカウントの account ID / role / values）
  を含めます。** ただし、そのサービスに存在する未選択のアカウントは含めません。
- **インポートは Vault 全体を置換しません。** `current.services = imported.services` のような
  全体置換は行わず、共有ファイルに含まれる対象（サービス／アカウント）だけを追加・更新します。
  個人サービス・個人アカウント・他の共有アカウント・利用者独自設定はそのまま維持されます。
  - サービスの同一判定は `service.id`。存在しなければ新規追加、存在すれば既存サービス全体は
    置換せず、URL 条件・入力項目・`sharedValues` を共有ファイルの内容でマージ（id ベースで
    追加・更新のみ。既存にしかない要素はそのまま残す）し、アカウントは共有対象だけを追加・更新します。
  - アカウントの同一判定は、同一サービス内の `account.id`。存在しなければ新規追加、存在すれば更新します。
    そのため、共有元でパスワードを変更して再共有した場合、受取側では重複したアカウントを作らずに
    同じアカウントが更新されます（同じファイル・同じ内容の再インポートでも重複しません）。
  - `field.id` を基準にマージするため、共有元と受取側で入力項目の構成が異なっていても、値を
    別の項目へ推測して移し替えることはありません。受取側だけが持つ項目・その値
   （共有対象外のアカウントが使っているもの）は保持されます。
  - 管理者（admin）区分はそのまま維持されます。
- **新しいロジックは `src/lib/share.js` に分離しました。** 選択データの抽出、共有ファイルの検証
 （形式・バージョン・パスフレーズ・破損の判別）、インポート計画の作成、マージ処理を担い、
  復号したデータは既存の `normalizeVault()` / `normalizeService()` を通して正規化・検証します。
  暗号化自体は既存の `lib/crypto.js` をそのまま再利用しています。
- 自動テスト（`test/share.test.js` / `test/vault-store.test.js`）を追加し、選択アカウントのみの
  抽出、未選択アカウントの除外、admin role の維持、0 件選択時の出力拒否、追加/更新/重複防止、
  未共有アカウントの保護、`sharedValues` / field ID の維持、不正ファイル・非対応バージョン・
  パスフレーズ不一致・形式の取り違えの拒否、Vault 全体の非置換、既存のマスターバックアップ
  保存・復元（replace/merge）の回帰を検証しています。
- Vault のデータ形式および `schemaVersion` は変更していません。共有ファイル独自の `format` /
  `version` で将来の互換性を管理します。

## v0.4.3 — capture値の重複反映防止

- capture結果とscan候補の双方で入力欄を一意に特定できる場合だけ、取得した値を初期反映するようにしました。
- 同名入力欄の片方だけに値がある場合、その値が別の空欄へ複製される問題を修正しました。
- 曖昧なため値を反映しなかった場合は、取り込み成功メッセージを表示しないようにしました。
- Vault のデータ形式および `schemaVersion` は変更していません。

## v0.4.2 — リリース履歴の分離

README から各バージョンのリリース履歴を `release-note.md` へ分離しました。機能および Vault のデータ形式は変更していません。


## v0.4.1 — 入力済み値取り込みの安全性・編集保護

v0.4.0 の入力済み値取り込みを、一意性を優先する対応付けに改めました。Vault のデータ形式は
変更していません（`schemaVersion` は変更なし）。

- frame、要素種別、フォーム位置、CSS パス、id、name を組み合わせ、capture 結果が一意な場合だけ
  初期値を反映します。同名候補などを一意に特定できない場合は、秘密項目を含め手入力を優先します。
- 文字入力、貼り付け、削除、空欄化を明示的な利用者編集として記録し、候補変更や再描画の後も
  capture 値で上書きしません。
- アカウント項目の capture 値は新規サービスの「標準ユーザー」だけへ反映し、後から追加した
  アカウントには複製しません。共通項目は従来どおり共通値へ取り込めます。
- scan と capture の分離、メモリ上だけの一時保持、既存サービスを capture 対象外とする安全策は
  そのまま維持しています。

## v0.4.0 — ログイン画面の入力済み値取り込み

新しいサービスを登録するときの二重入力（ログイン画面への入力 → 設定ページへの再入力）を解消しました。
Vault のデータ形式は変更していません（`schemaVersion` は変更なし。取得した値は、既存の
`account.values` / `sharedValues` へ通常の入力と同じ形で入るだけです）。

- **新規サービスの設定を開始すると、ログイン画面に入力済みの値が「標準ユーザー」アカウントの
  初期値として自動的に反映されるようになりました。**（詳細は 5.5）
  - 走査（候補一覧の作成、scan）とは別の処理（capture）として実装し、常駐監視は追加していません。
    値を取得するのは、利用者が「このログイン画面を設定」を実行した直後の 1 回だけです。
  - 値の対応付けは、走査候補と同じ `id` → `name` → CSS セレクタの照合と、フレーム情報を使います。
    単純に「最初の text 欄」「最初の password 欄」とは対応付けません。
  - 空欄の入力欄は取り込みません。取得に失敗しても、走査に成功していれば従来どおり手入力で登録できます。
  - 取得した値はページを離れる（保存する）までメモリ上の下書きとして扱い、`chrome.storage` 等の
    一時ストレージへ平文で保存することはありません。設定ページを閉じる・読み込み直すと消えます。
  - 設定ページで「対象入力欄」を切り替えた場合は取り込んだ値も追従し、利用者が値を手入力で
    修正した後はその内容を上書きしません。
  - 既存サービスの設定を編集する場合は取得しません（登録済みの値を無確認で置き換えないため）。

## v0.3.3 — 不具合修正と整理

日常の利用で支障となる不具合の修正と、読みやすさのための整理を行いました。
Vault のデータ形式は変更していません（`schemaVersion` は変更なし）。

**不具合修正**

- **チェックボックス / ラジオボタンの項目で、入力時に `input` / `change` を二重に発火していたのを修正しました。**
  クリック操作自体がこれらのイベントを発火するにもかかわらず、続けて同じイベントを送っていたため、
  `onchange` で入力チェックを行う業務システムで検証が二度走っていました（v0.3.1 で修正した `blur` の
  二重発火と同じ原因です）。
- **ラジオボタンの項目で、登録値と一致しないのに「入力しました」と表示していたのを修正しました。**
  ラジオボタンはクリックでは解除できないため実際には何も変わっておらず、区分が切り替わっていないことに
  気付けませんでした。この場合は入力できなかったものとして表示します。
  併せて、状態が変わらない無意味なクリックをページへ送らないようにしました。
- **項目名の自動判定が効かない画面があったのを修正しました。**
  `<label><input></label>` のように文字を持たないラベルが付いていると、そこで手掛かり探しが
  打ち切られ、表のセルや直前の見出しからラベルを拾えていませんでした。中身のある文字列だけを採用します。
  項目名の推定に加えて、入力欄の再特定の精度も上がります。
- **ログイン画面設定ページで、設定を読み込めなかったときに「再スキャン」や「保存」を押すと
  操作が何も起きなくなる不具合を修正しました。** 読み込めていない旨を表示します。
- **「アカウントを追加」の既定名が実際の件数とずれていたのを修正しました。**
  「標準ユーザー」1 件だけのサービスで最初に追加すると「アカウント2」になっていました。
  使われていない最も小さい番号を使います。
- **アカウントの自動保存で、保存していないのに「保存済み」と表示することがあったのを修正しました。**
  保存が実行されるまでに別のサービスへ切り替えた場合、その内容は保存しません。

**整理（動作は変わりません）**

- 「確認」（入力欄の強調表示）が失敗したときの案内を理由ごとの一覧にまとめ、
  フレーム構成を確認できなかった場合に「入力欄が見つかりません」と誤って案内していたのを改めました。
- ページ内処理の呼び出しを `runPageAgent()` 一つにまとめ、全フレーム実行と単一フレーム実行は
  その薄い呼び分け（`runInAllFrames()` / `runInFrame()`）にしました。同じ内容の例外処理が
  3 か所に重複していたのを解消しています。
- 走査できなかったフレームの判定を `hasUnreachableFrames()` として切り出し、`scanPage()` を短くしました。
- 値の反映処理（`applyValue()`）を、入力欄の種類ごとに「値を入れる」「ページへ通知する」が
  1 か所で読み取れる形へ整理しました。

## v0.3.2 — 不具合修正と整理

日常の利用で支障となる不具合の修正と、読みやすさのための整理を行いました。
Vault のデータ形式は変更していません（`schemaVersion` は変更なし）。

**不具合修正**

- **入力欄の対応付けが弱い項目が、他の項目の入力欄を先取りしてしまう不具合を修正しました。**
  登録順にそのまま割り当てていたため、画面改修などで対応付けが弱くなった項目が、
  確実に特定できる別項目の入力欄を取ってしまい、値が入れ替わって入力される場合がありました。
  確実に特定できた項目から先に割り当てます。
- **URL条件のパスに先頭の `/` を付け忘れると、どのページにも一致しなくなる不具合を修正しました。**
  `login` のように入力しても `/login` として扱います。誤って付けたクエリ（`?` 以降）も取り除きます。
  入力欄には整えた結果を表示するので、保存前に確認できます。
- **「確認」（入力欄の強調表示）を続けて押すと、オレンジ色の枠が消えなくなる不具合を修正しました。**
  強調表示中の見た目を「元の見た目」として覚えてしまっていました。
- **既存サービスの設定を開いたとき、1 つの入力欄が複数の項目に割り当たることがあったのを修正しました。**
  そのままでは保存時にエラーとなり、手作業で直すまで保存できませんでした。
  既に他の項目へ割り当てた入力欄は、対応付けの候補から外します。
- **サービス一覧のアカウント件数が、アカウントの自動保存後も古いままだったのを修正しました。**
  一覧の表示だけを更新するため、編集中の内容には影響しません。

**整理（動作は変わりません）**

- ログイン画面設定ページで、入力項目を組み立てる処理が「保存時」と「画面の描画時」に
  ほぼ同じ内容で二重に書かれていたのを、`collectFields()` 一つにまとめました。
- フレームの再特定を `probeFrames()`（構成の確認）と `matchFrames()`（該当フレーム探し）に分け、
  入力 1 回につきフレーム構成の確認を 1 度だけ行うようにしました。
  併せて、フレームを絞り込めた場合と絞り込めなかった場合で分かれていた入力処理を
  `fillFrameGroup()` 一つにまとめました。

## v0.3.1 — 不具合修正と整理

日常の利用で支障となる不具合の修正と、読みやすさのための整理を行いました。
Vault のデータ形式は変更していません（`schemaVersion` は変更なし）。

**不具合修正**

- **入力欄を特定しきれずに見送った秘密項目の入力欄へ、別項目の値が入ることがある不具合を修正しました。**
  弱い一致のため入力を見送ったパスワード欄が「未使用」のままだったため、後続の通常項目がその欄を
  掴んでしまう場合がありました。項目に割り当てた入力欄は、入力の可否によらず他の項目へ渡しません。
- **入力時に `blur` を二重に発火していたのを修正しました。** `blur()` の呼び出しに加えて合成イベントも
  送っていたため、`onblur` で入力チェックを行う業務システムで検証が二度走っていました。
- **入力結果の一覧が登録した項目の順に並ばない不具合を修正しました。** フレームごとに入力するため
  結果が前後していました。登録順に並べ直し、項目と結果が 1 対 1 で対応するようにしています。
- **入力項目の表示名を打つたびにアカウント欄が作り直される問題を修正しました。** 入力欄の多い画面で
  設定画面が重くなり、「表示」にしていた秘密項目が入力途中で隠れてしまう原因になっていました。
  値の入力欄は、項目の内容が確定した時点で作り直します。
- **入力欄の候補が多い画面で設定画面が固まる問題を改善しました。** 「対象入力欄」の選択肢を行ごとに
  組み立て直していたのをやめ、候補 1 件につき 1 回だけ組み立てて各行へ複製するようにしました。
- **ログイン画面設定ページの読み込みに失敗したとき、原因によらず「ロックされています」と表示していたのを
  修正しました。** サービスが見つからない等、アンロックしても解決しない原因をそのまま表示します。
- **「アカウントを追加」の既定名が既存のアカウントと重複することがあったのを修正しました。**
  件数で採番していたため、削除後に追加すると同じ名前になる場合がありました。

**整理（動作は変わりません）**

- `match.js` と `frame.js` で重複していた URL の解釈とパスの正規化を `lib/url.js` に集約しました。
- アカウント自動保存の値の組み立て（`buildSafeAccountsPayload`）を、「直前保存時の値を土台にして、
  区分が変わっていない項目だけ現在の値で上書きする」という一つの規則に書き直しました。
- `frame.js` に紛れ込んでいた制御文字（NUL）を通常の区切り文字に置き換えました。
  ソースがバイナリ扱いになり、差分を確認できなくなっていました。

## v0.3.0 — フレーム内ログインフォーム対応

`frameset` / `frame` を使った古い業務システムや `iframe` でログインフォームを埋め込んだ画面に対応しました。
Vault のデータ形式は変更していません（`schemaVersion` は変更なし。既存項目にフレーム情報が無い場合は
自動的にトップフレーム扱いとして読み込まれます）。

- **走査（スキャン）が全フレームを対象になりました。** トップページに加えて、アクセス可能な
  `frame` / `iframe` すべてで入力欄を探し、候補として集約します。`<frame>` と `<iframe>` は区別しません。
- **項目ごとにフレーム情報を保存するようにしました。** 対象入力欄がどのフレーム（origin・パス・フレーム名）に
  属するかを保存し、入力の都度、現在のフレーム構成から対象フレームを再特定します。Chrome のフレーム番号
  （frameId）はタブを開き直すたびに変わり得るため、保存データには使っていません。
- **同じ推定名の入力欄が複数フレームにまたがる場合、自動では確定しません。** setup.html の候補一覧には
  `[フレーム: ○○]` の補足を表示し、利用者が使用する項目を選べるようにしました。
- **フレームを一つに絞り込めない場合、秘密項目は入力しません（fail closed）。** 対象フレームが見つからない、
  または同程度に一致するフレームが複数ある場合、既存の「locator の弱一致では秘密項目を入力しない」方針を
  フレームの特定にも適用しています。通常項目のみ、参考として弱い一致で入力を試みます。
- **入力直前にフレーム自身の URL も再確認するようにしました。** トップページの URL 確認（background 側・
  ページ側の二重確認）はそのまま維持したうえで、子フレームへ入力する場合は、そのフレーム自身の
  URL（origin + パス）も入力の直前に再確認します。query 文字列は比較対象に含めていません。
- 「強調表示」「再スキャン」も全フレーム対応にしました。再スキャンは frameId の一致に頼らず、
  フレーム自身の URL・名前で既存の対応付けをできるだけ保持します。
- 走査したフレームの数が想定より少ない場合（アクセスできなかったフレームがある可能性がある場合）は、
  候補が 0 件のときと区別して「一部のフレームを確認できませんでした」と表示するようにしました。
- background 側のフレーム再特定・入力ロジックは `runPageAgentAllFrames` / `runPageAgentInFrame` として
  整理し、`page-agent.js` 自体の scan / fill / highlight という自己完結した構造は維持しています。

## v0.2.2 — 自動保存とサービス設定の状態分離

v0.2.1 のレビューで、未保存の「サービス設定」（入力項目の区分変更・削除）が
アカウントの自動保存に混ざる可能性があるという指摘を受けて修正しました。
Vault のデータ形式・暗号化方式の変更はありません（`schemaVersion` は変更なし）。

- **未保存の入力項目変更が、アカウントの自動保存に巻き込まれないようにしました。**
  入力項目の区分（共通 / アカウント）を変更したり項目を削除したりすると、画面上は
  即座に値が移動・消去されて見えます（この見た目自体は従来どおりです）。しかし
  これらは「サービス設定」の一部で明示保存が前提のため、保存されるまでの間に
  アカウント名の変更などで自動保存が走っても、影響を受けた値は**直前に保存済みの
  内容のまま**送るようにしました。「サービス設定を保存」（または setup.html の
  「保存」）を押して初めて、区分変更・削除がアカウント値にも反映されます。
- **明示的な「保存」は、クリックした時点の内容で確定してから送るようにしました。**
  保存ボタンを押した処理がアカウントの自動保存の後ろに並んでいる間に、さらに
  URL・入力項目を編集しても、その保存クリックが後から編集した内容まで巻き込んで
  保存することはありません。
- この判定・復元ロジック（`buildSafeAccountsPayload`）と、保存直後のスナップショット
  作成（`snapshotService`）は `src/ui/account-editor.js` に共通化し、setup.html /
  options.html の双方が同じ実装を利用しています。

## v0.2.1 — アカウント変更の自動保存

アカウントの追加・編集は日常的に行う操作のため、保存忘れが起きないようにしました。
Vault のデータ形式・暗号化方式の変更はありません（`schemaVersion` は変更なし）。

- **既に保存済みのサービスでは、アカウントの追加・削除・名前変更・通常/管理者区分の変更・
  各項目値（ID・パスワード・第二パスワード等）の変更を自動保存するようにしました。**
  setup.html・options.html のどちらでも、アカウントを変更したあとに改めて「保存」を押す必要は
  ありません。テキスト入力は打鍵のたびにではなく、確定（`change` / フォーカスが外れたタイミング）
  で保存します。追加・削除・区分変更のように操作が明確に確定するものはその場で保存します。
- **対象URL・入力項目・サービス共通値・サービス名は、従来どおり明示的な「保存」のままです。**
  自動保存はアカウントのみを対象とし、これらの項目を保存するには options.html の
  「サービス設定を保存」（旧「保存」）ボタン、または setup.html の「保存」ボタンを押してください。
  アカウントの自動保存が、保存されていないURL・入力項目の変更を巻き込んで保存することはありません。
- **新規サービスは最初の1回だけ明示保存が必要です。** サービスIDが確定していない新規サービスでは
  アカウントを変更しても自動保存されません。最初の保存が完了した後は、そのままの画面でアカウントを
  追加・編集すると自動保存されます。
- アカウント領域に小さな保存状態表示（「保存中…」「保存済み」）を追加しました。保存に失敗した場合は
  「保存できませんでした」と明確に表示し、`[再試行]` から再送できます。失敗した入力内容が画面から
  消えることはありません。
- 保存処理は直列化されており、短時間に複数の変更が発生しても、古い保存処理が後から完了して
  新しい値を上書きすることはありません。常に最後に確定した内容が Vault へ反映されます。
- 自動保存のトリガーと直列保存キューは `src/ui/account-editor.js` に共通化し、setup.html /
  options.html の双方から同じ実装を利用しています。

## v0.2.0 — アカウント管理導線の再設計

日常利用で最も操作するアカウント管理を、より近く・分かりやすくしました。
Vault のデータ形式・暗号化方式・URL 判定ロジックの変更はありません（`schemaVersion` は変更なし）。

- **ログイン画面設定ページ（setup.html）だけでアカウントまで設定できるようにしました。**
  これまではサービス登録後、2 件目以降のアカウントを追加するには「サービス一覧」（options.html）へ
  移動する必要がありましたが、設定ページ内にアカウント一覧（追加・削除・通常/管理者区分・各項目値の編集）
  を追加し、ログイン画面の解析から保存まで 1 画面で完結するようにしました。既存サービスを
  設定ページから開いた場合も同様にアカウントを編集できます。
- **サービス一覧（options.html）で、アカウントを最初に表示するようにしました。**
  サービスを選択した直後に表示されるのはアカウント（追加・削除・ID/パスワード変更・通常/管理者区分）で、
  対象URL・入力項目・サービス共通値は「サービス設定」として 1 つに折り畳み、初期状態では閉じています。
  日常的にはほとんど参照しない情報が先に表示され、アカウントまでスクロールする必要があった状態を解消しました。
- アカウント一覧の描画・追加・削除・値編集・秘密項目の表示切り替えを共通の部品（`src/ui/account-editor.js`）
  にまとめ、設定ページとサービス一覧の双方から同じ実装を利用するようにしました。
- 管理者アカウントに関する安全策（既定で未選択・明示確認が必要・background 側での確認検証）は
  従来どおり維持しています。

## v0.1.4 — 不具合修正と整理

業務中に実際に支障となる不具合を修正しました。データ形式の変更はありません。

- **ログイン画面設定ページで「保存」を 2 回押すと、登録した値がすべて消えていた問題を修正**しました。
  2 回目の保存で入力項目が別物として扱われ、値の対応付けが外れていたためです。
  保存後は項目の対応付けを保持し、続けて保存し直しても値はそのまま残ります。
- **同じ画面で「保存」を続けて押すと、同じサービスが二重に登録されることがあった問題を修正**しました。
  保存中はボタンを押せないようにしています。
- **項目の区分（共通 / アカウント）を変更すると、アカウントごとに登録していた値が失われていた問題を修正**しました。
  変更前の区分の値を消さずに残すため、区分を戻せば各アカウントの値が元のまま使えます。
- **設定ページで「再スキャン」すると、入力中の項目や手動で追加した項目が消えていた問題を修正**しました。
  行は作り直さず「対象入力欄」の選択肢だけを更新します。表示名を書き換えていても値は失われません。
- **入力欄の多いログイン画面で、走査・入力に時間がかかり画面が固まったようになる問題をさらに改善**しました。
  入力欄ごとにページ全体を数え直していた処理をやめています（入力欄 300 件の画面で約 1/4 の時間）。
  入力欄の識別情報は従来と同一で、登録済みの設定はそのまま使えます。
- **ログイン画面設定ページで対象URLの条件を書き換えても保存されなかった問題を修正**しました。
  条件欄を触っていない場合に条件を重複追加しない動作は従来どおりです。
- 走査結果をタブごとに保持し続けていたのをやめ、直前の 1 件だけを保持するようにしました。
  別のタブの走査結果が設定ページへ渡ることもなくなります。

## v0.1.3 — 不具合修正と整理

業務中に実際に支障となる不具合を修正しました。データ形式の変更はありません。

- **入力欄の多いログイン画面で、入力に数秒かかり画面が固まったようになる問題を修正**しました。
  入力欄の識別情報を項目ごとに作り直していたのをやめ、1 回だけ作って使い回すようにしています
  （入力欄 300 件の画面で約 1.9 秒 → 約 0.3 秒）。入力先の判定結果は従来と同じです。
- **設定画面で「再スキャン」すると入力済みの値が消えていた問題を修正**しました。表示名が同じ項目の値を引き継ぎます。
- **項目の区分（共通 / アカウント）を変更すると、登録済みの値が失われていた問題を修正**しました。
  変更後の区分側へ値を引き継ぎます（誤って変更した場合も、戻せば元の値のまま使えます）。
- **サービス一覧からの保存時に、サービス名・オリジンが未入力でも保存できてしまう問題を修正**しました。
  どの URL にも一致しないサービスが気付かないうちに出来上がることがなくなります。
  オリジン欄の正規化は入力途中ではなく確定時に行い、結果を画面へ反映します。
- **設定画面の「確認」ボタンが、入力欄を特定できなかった場合でも成功と表示していた問題を修正**しました。
- popup の表示を改善しました。対象ページをプロトコル込み（`https://…`）で表示し、
  サービス / アカウントを切り替えた際に前回の入力結果を消します。
  また、入力先が確実でない項目がある場合は、その件数を入力結果に表示します。
- バックアップ復元時、ファイルの形式が違う場合に「パスフレーズが違います」と誤って案内していた点を改めました。
- バックアップファイル名の日時を端末のローカル時刻にしました。
- サービス一覧の操作中にロックされた場合など、処理に失敗した際の内容を画面へ表示するようにしました。

## v0.1.2 — 旧 URL 条件の実 URL ベース自動移行

- v0.1.0 形式（プロトコル情報を持たない `hostname`）の URL 条件を `https` とみなす暗黙変換を廃止しました。
  lgpwmng がプロトコルを推測することはありません。
- プロトコル未確定の条件はそのまま保持し（`schemaVersion` 3）、対象のログイン画面を実際に開いた時点で、
  そのページのオリジンを採用して新形式へ自動移行し、Vault へ保存します。サブドメイン一致の条件も意味を保って移行します。
- 現在のページが旧条件に一致しない場合は、条件を書き換えず、認証情報も入力しません。
  移行後も、入力時には通常どおり URL の再検証を通します。

## v0.1.1 — 認証情報入力時の安全性補強

- 入力の直前に URL を再確認するようにしました。background で現在のタブ URL を取得してサービスの
  条件と照合し、注入したコード自身もページの `location` を再確認します（不一致・URL 不明なら入力しません）。
- URL 判定を `hostname + pathname` から **`origin + pathname`** へ変更しました。
  プロトコルとポートを含めて判定するため、`http` と `https` を区別します。
  旧形式で保存された条件は、読み込み時に `https` のオリジンとして移行されます。
- 秘密項目（種別「秘密」）は、入力欄の特定が確実な場合のみ入力するようにしました。
  一致が弱い場合は入力せず、設定の更新を促します（通常項目は従来どおり警告付きで入力します）。

## v0.1.0

- 初版。
